Place two sample files in this folder before running the test upload:
- sample.pdf
- sample.docx

You can create small files for testing. The script uses environment variable ADMIN_TOKEN for authorization and API_BASE if needed.
